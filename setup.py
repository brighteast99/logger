import setuptools

setuptools.setup(
    name="logger_initializer",
    version="1.0",
    packages=setuptools.find_packages(),
    python_requires=">=3.6",
    include_package_data=True,
    package_dir={"logger_initializer": "logger_initializer"},
)
